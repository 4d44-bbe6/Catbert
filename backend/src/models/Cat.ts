class Cat {
  private id: string;
  private name: string;
  private rfid: string;

  constructor(id: string, name: string, rfid: string) {
    this.id = id;
    this.name = name;
    this.rfid = rfid;
  }
}

export default Cat;
